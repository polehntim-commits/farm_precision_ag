"""Stub tests for USDA Query Log immutability.

TODO(Phase 2): once a bench test site exists, assert that a second save on an
existing log row raises, and that on_trash is blocked.
"""

import unittest


class TestUSDAQueryLog(unittest.TestCase):
    def test_placeholder(self):
        self.assertTrue(True)
